# Founder Email — PARKED

**Status: DO NOT SEND.**

A Razorpay founder gave direct email access and asked to see built projects.
This is the highest-priority channel because of direct access — which is
exactly why it must not be spent on an unlocked thesis. Per
`docs/context/transfer.md` §0 and `CLAUDE.md` rule 8:

> The founder email is deliberately parked until the Buildathon project
> thesis is locked — do not write it early.

Unlock condition: a problem-lock ADR exists in `docs/decisions/`, ideally with
the Phase 3–4 spine (`src/`) and evaluation numbers (`eval/`) already real, so
the email references a shipped system rather than an intention.

Draft below this line once unlocked. Leave this line and the status above
intact until then.

---
