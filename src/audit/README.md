# audit

`ledger.py` — `AuditLedger`, append-only hash-chained JSONL with
`verify_chain()` for tamper detection. Deepens Day 10 (replay, queryability)
— see ../README.md for depth allocation and status.
