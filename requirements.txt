pydantic>=2.6
pytest>=8.0
pytest-cov>=5.0

# Only required for src/agent/reasoner.py's LLMReasoner path (activates when
# ANTHROPIC_API_KEY is set). NaiveReasoner and the rest of the vertical
# slice run without it.
anthropic>=0.40
